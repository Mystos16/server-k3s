apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: infra

resources:
  - deployment.yaml
  - configmap.yaml
  # The tunnel credentials are a SealedSecret you generate with kubeseal
  # (see README). Uncomment once created:
  # - sealed-credentials.yaml
