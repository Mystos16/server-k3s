apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: auth

resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  # SealedSecret 'lldap-secrets' (JWT_SECRET, LDAP_USER_PASS) — create with kubeseal
  # - sealed-secret.yaml
