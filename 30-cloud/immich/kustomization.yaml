apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cloud

resources:
  - db.yaml
  - redis.yaml
  - server.yaml
  - machine-learning.yaml
  - service.yaml
  - ingress.yaml
  # SealedSecret 'immich-secrets' (db-password) — kubeseal
  # - sealed-secret.yaml
