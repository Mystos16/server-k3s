apiVersion: v1
kind: Service
metadata:
  name: shelfmark
  namespace: downloads
  labels:
    app: shelfmark
spec:
  selector:
    app: shelfmark
  ports:
    - name: http
      port: 80
      targetPort: 8084
