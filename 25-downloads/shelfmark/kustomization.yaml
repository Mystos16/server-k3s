apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: downloads

resources:
  - deployment.yaml
  - service.yaml
  # Public via cloudflared; internal ingress also provided for LAN.
  - ingress.yaml
