apiVersion: v1
kind: Service
metadata:
  name: minecraft
  namespace: games
  labels:
    app: minecraft
spec:
  type: LoadBalancer
  selector:
    app: minecraft
  ports:
    - name: mc
      port: 25565
      targetPort: 25565
      protocol: TCP
