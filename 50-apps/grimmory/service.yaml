apiVersion: v1
kind: Service
metadata:
  name: grimmory
  namespace: apps
  labels:
    app: grimmory
spec:
  selector:
    app: grimmory
  ports:
    - name: http
      port: 80
      targetPort: 6060
