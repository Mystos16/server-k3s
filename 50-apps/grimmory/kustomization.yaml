apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: apps

resources:
  - db.yaml
  - deployment.yaml
  - service.yaml
  # Public via cloudflared; internal ingress also provided.
  - ingress.yaml
  # SealedSecret 'grimmory-secrets' (db-password) — kubeseal
  # - sealed-secret.yaml
